# matrix

Веб-приложение на React + TypeScript + Vite.

## Команды

```bash
yarn install   # установить зависимости
yarn dev       # запуск дев-сервера
yarn build     # сборка в dist/
yarn preview   # предпросмотр прод-сборки
yarn lint      # проверка ESLint
```
